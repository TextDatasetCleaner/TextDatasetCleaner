PRE_PROCESSORS:
  - unique
PROCESSORS:
  - line_strip
  - clean_html:
      or_condition: False
  - detect_language:
      language_code: ru
      delimiter: '~'
      delimited_position: -1
  - filter_textacy
POST_PROCESSORS:
  - unique
  - shuffle
